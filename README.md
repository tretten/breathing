# Wim Hof Breathing App

Синхронное веб-приложение для совместного дыхания по методу Вима Хофа.

## 🚀 Быстрый старт

### 1. Установка зависимостей
```bash
npm install
```

### 2. Настройка Firebase
1. Создайте проект на [Firebase Console](https://console.firebase.google.com/)
2. Включите Realtime Database
3. Скопируйте `.env.example` в `.env.local`:
   ```bash
   cp .env.example .env.local
   ```
4. Заполните `.env.local` своими Firebase credentials

### 3. Настройка Firebase Rules
Загрузите правила безопасности из `database.rules.json`:
```bash
firebase deploy --only database
```

### 4. Добавьте аудиофайлы
Положите MP3 файлы в `public/audio/`:
- `ru_4rounds.mp3` — русский, 4 раунда
- `en_4rounds.mp3` — английский, 4 раунда
- `ru_3rounds.mp3` — русский, 3 раунда
- `en_3rounds.mp3` — английский, 3 раунда

### 5. Запуск
```bash
npm run dev
```

Откройте http://localhost:5173

## 📱 Функции

### Автокомнаты (ru_4rounds, en_4rounds, ru_3rounds, en_3rounds)
- Сессии запускаются каждые 30 минут (в :00 и :30)
- Синхронное воспроизведение аудио для всех участников
- Таймер обратного отсчёта

### Custom Room
- Выбор любого из 4 пресетов
- Запуск по готовности всех участников
- Countdown 3-2-1 перед стартом

## 🛠 Технологии

- **Frontend**: React 18 + TypeScript + Vite
- **Backend**: Firebase Realtime Database (бесплатный tier)
- **Audio**: Web Audio API
- **Styling**: CSS с CSS Variables

## 📁 Структура проекта

```
src/
├── components/     # UI компоненты
├── firebase/       # Firebase конфигурация
├── hooks/          # React хуки
├── pages/          # Страницы (роуты)
├── styles/         # CSS стили
├── types/          # TypeScript типы
└── utils/          # Утилиты и константы
```

## 🔧 Скрипты

```bash
npm run dev      # Запуск dev сервера
npm run build    # Production сборка
npm run preview  # Предпросмотр сборки
npm run lint     # Проверка кода
```

## 📝 License

MIT
